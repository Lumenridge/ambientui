/**
 * THE ROOT ENTRY — `import { AMBIENT_COMPONENTS } from "@ambientui/docs"`.
 *
 * The catalog is the package's one code export; the governing documents ship
 * as raw markdown under ./content/* (resolve them by their repo path, e.g.
 * `@ambientui/docs/content/docs/motion-spec.md`).
 */
export * from "./catalog"
