{
  "name": "ambientui",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "gate": "npm run registry:check && npm run vendor:check && turbo typecheck lint build",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "registry:build": "node scripts/build-registry.mjs && shadcn build --output apps/web/public/r",
    "registry:check": "node scripts/build-registry.mjs --check",
    "vendor:check": "node scripts/check-vendored-css.mjs",
    "release": "turbo build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.18",
    "typescript": "~6"
  },
  "packageManager": "npm@10.9.3",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
